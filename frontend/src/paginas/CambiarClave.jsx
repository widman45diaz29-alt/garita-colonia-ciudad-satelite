import {useState} from 'react';import {api} from '../servicios/api';import Mensaje from '../componentes/Mensaje'
export default function CambiarClave({actualizarSesion,obligatorio=false}){const[f,setF]=useState({clave_actual:'',nueva_clave:'',confirmar:''});const[msg,setMsg]=useState('');const[err,setErr]=useState('');const enviar=async e=>{e.preventDefault();setMsg('');setErr('');if(f.nueva_clave!==f.confirmar){setErr('La nueva contraseña no coincide');return}try{const s=await api.cambiarClave({clave_actual:f.clave_actual,nueva_clave:f.nueva_clave});localStorage.setItem('sesion_garita',JSON.stringify(s));actualizarSesion(s);setMsg('Contraseña actualizada')}catch(x){setErr(x.message)}};return <section className='tarjeta compacta'><h2>{obligatorio?'Actualiza tu contraseña':'Cuenta'}</h2><form onSubmit={enviar} className='cuadricula'><input type='password' placeholder='Contraseña actual' value={f.clave_actual} onChange={e=>setF({...f,clave_actual:e.target.value})} required/><input type='password' placeholder='Nueva contraseña' value={f.nueva_clave} onChange={e=>setF({...f,nueva_clave:e.target.value})} required/><input type='password' placeholder='Confirmar contraseña' value={f.confirmar} onChange={e=>setF({...f,confirmar:e.target.value})} required/><button>Cambiar contraseña</button></form><Mensaje texto={msg}/><Mensaje texto={err} tipo='error'/></section>}
